# BasicTokenFlow

main: kit: flow: BasicTokenFlow:
{
  entity: "token",
  kind: "basic",
  name: "BasicTokenFlow",
  step: [
    {
      input: {
        ref: "token_ref01"
      }

      op: "create"
    }
  ],
  "key$": "BasicTokenFlow"
}